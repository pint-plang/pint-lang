let main() -> unit {
  printsln("Hello World!");
  let n: int := aski("Please enter a number: ");
  printi(n);
  prints("! = ");
  printiln(factorial(n));
}

let x: int := 42;

let factorial(n: int) -> int {
  if n not <= 1 then {
    n * factorial(n - 1)
  } else {
    1
  }
}